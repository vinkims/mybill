import {StyleSheet, Dimensions} from 'react-native';

const {wdth, height} = Dimensions.get('screen');

export default globalStyles = StyleSheet.create({
    container:{
        alignItems: 'center',
        paddingTop: 30
    }
}); 